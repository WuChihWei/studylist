{
  "name": "studylist-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd server && node dist/index.js",
    "build": "cd server && npm install && npm run build",
    "dev": "cd server && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "react-hot-toast": "^2.5.2"
  }
}
